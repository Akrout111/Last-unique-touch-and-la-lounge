# ✅ مراجعة تقرير المرحلة 7 — ممتاز

تنفيذ شامل واحترافي. كل الصفحات القانونية جاهزة للتوثيق:

| النقطة | التقييم |
|--------|--------|
| 8 ملفات Markdown منفصلة (عربي + إنجليزي) | ✅ صيانة سهلة |
| 13 قسم في الشروط + 13 في الخصوصية | ✅ محتوى احترافي للتوثيق |
| `sitemap.ts` و `robots.ts` مُولّدان تلقائياً | ✅ تحضير ممتاز للـ SEO |
| `content.ts` helper مع fallback للعربية | ✅ UX مدروس |
| إزالة تعارض `public/robots.txt` | ✅ تشخيص دقيق |

---

# 🚀 البرومبت الثامن — لوحة التحكم للأدمن

انسخ البرومبت التالي وأرسله للـ Full Stack Agent:

---

````markdown
# مهمة: لوحة التحكم للأدمن — المرحلة 8

## 📖 السياق والمراجع الإلزامية

### طبيعة هذه المرحلة
أنت في **المرحلة 8 من 10**. المرحلة 7 (الصفحات القانونية) اكتملت بنجاح. الآن نبني **لوحة تحكم الأدمن** — حيث يدير صاحب العمل المنتجات والحجوزات والفئات. هذه اللوحة محمية بـ **مصادقة بسيطة (mock auth)** لأن Clerk سيُضاف في المرحلة 10.

### ⚠️ قيود بيئية (مهمة)
1. **استخدم `bun dev --webpack -p 3000`** لتشغيل المشروع
2. **SQLite فقط**
3. **🚨 لا تستخدم Clerk** — سنستخدم mock auth بسيط (cookie-based)
4. **لا رفع صور فعلي** — استخدم URL input (سنضيف رفع الصور في المرحلة 10 مع Supabase Storage)
5. **استخدم Server Actions** للتغييرات في DB (أنظف من API routes للوحة التحكم)

### المراجع الإلزامية (اقرأها قبل البدء)
1. ملف `GLM-5.2-FullStack-Build-Reference.md`
2. ملف `GLM-5.2-Assistant-Operations-Reference.md` — **خصوصاً الأخطاء الأمنية #7, #9, #10, #14, #15, #16**
3. **اقرأ الكود الحالي قبل البدء**:
   - `prisma/schema.prisma` — خصوصاً `Product`, `Category`, `Booking`, `BookingStatus`
   - `src/lib/products.ts` — الـ helpers الحالية
   - `src/lib/cart.ts` — خصوصاً `parseImages`, `stringifyImages`
   - `src/app/[locale]/admin/page.tsx` — placeholder حالياً
   - `messages/ar.json` و `messages/en.json`

### البنية المستهدفة
```
/admin/login                   → صفحة تسجيل الدخول (mock auth)
/admin                         → Dashboard (إحصائيات + أحدث الحجوزات)
/admin/products                → قائمة المنتجات + بحث + فلترة
/admin/products/new            → إضافة منتج جديد
/admin/products/[id]/edit      → تعديل منتج
/admin/categories              → إدارة الفئات
/admin/bookings                → قائمة الحجوزات + فلترة بالحالة
/admin/bookings/[id]           → تفاصيل حجز + تغيير الحالة
```

---

## 🎯 هدف هذه المهمة

بناء **لوحة تحكم كاملة** لصاحب العمل تشمل:
1. Mock auth (cookie-based) — حماية `/admin/*`
2. صفحة Dashboard بإحصائيات (عدد المنتجات، الحجوزات، الإيرادات)
3. CRUD كامل للمنتجات (Create, Read, Update, Delete)
4. CRUD للفئات
5. إدارة الحجوزات (عرض، فلترة، تغيير الحالة)
6. UI احترافي مع sidebar navigation
7. تأكيدات الحذف (modal)
8. Toast notifications للنجاح/الفشل

---

## 🔧 المهام المطلوبة (بالترتيب)

### 1️⃣ Mock Auth Library
- [ ] أنشئ `src/lib/auth.ts`:

```typescript
import { cookies } from 'next/headers';
import { createHmac } from 'crypto';

const SESSION_COOKIE = 'lut_admin_session';
const SESSION_MAX_AGE = 60 * 60 * 24 * 7; // 7 days

/**
 * Verify admin password against env var.
 * In production (Phase 10), this will be replaced by Clerk.
 */
function getAdminPassword(): string | null {
  return process.env.ADMIN_PASSWORD || null;
}

function createSessionToken(): string {
  const secret = process.env.ADMIN_PASSWORD || 'fallback-secret';
  const timestamp = Date.now().toString();
  const signature = createHmac('sha256', secret).update(timestamp).digest('hex');
  return `${timestamp}.${signature}`;
}

function verifySessionToken(token: string): boolean {
  if (!token || !token.includes('.')) return false;
  const [timestamp, signature] = token.split('.');
  const secret = process.env.ADMIN_PASSWORD || 'fallback-secret';
  const expectedSignature = createHmac('sha256', secret).update(timestamp).digest('hex');
  
  // Check signature
  if (signature !== expectedSignature) return false;
  
  // Check expiry
  const age = Date.now() - parseInt(timestamp, 10);
  if (age > SESSION_MAX_AGE * 1000) return false;
  
  return true;
}

export async function login(password: string): Promise<boolean> {
  const adminPassword = getAdminPassword();
  if (!adminPassword) {
    // In development without ADMIN_PASSWORD set, allow "dev" as password
    if (process.env.NODE_ENV === 'development' && password === 'dev') {
      const cookieStore = await cookies();
      cookieStore.set(SESSION_COOKIE, createSessionToken(), {
        httpOnly: true,
        secure: process.env.NODE_ENV === 'production',
        sameSite: 'lax',
        maxAge: SESSION_MAX_AGE,
        path: '/',
      });
      return true;
    }
    return false;
  }
  
  if (password !== adminPassword) return false;
  
  const cookieStore = await cookies();
  cookieStore.set(SESSION_COOKIE, createSessionToken(), {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: SESSION_MAX_AGE,
    path: '/',
  });
  return true;
}

export async function logout(): Promise<void> {
  const cookieStore = await cookies();
  cookieStore.delete(SESSION_COOKIE);
}

export async function isAuthenticated(): Promise<boolean> {
  const cookieStore = await cookies();
  const token = cookieStore.get(SESSION_COOKIE)?.value;
  if (!token) return false;
  return verifySessionToken(token);
}

/**
 * Throws or redirects if not authenticated.
 * Use in Server Components / Server Actions.
 */
export async function requireAuth(): Promise<void> {
  const authed = await isAuthenticated();
  if (!authed) {
    throw new Error('UNAUTHORIZED');
  }
}
```

- [ ] أضف في `.env.example`:
  ```bash
  # Admin (mock auth — will be replaced by Clerk in Phase 10)
  ADMIN_PASSWORD=""
  ```

- [ ] في `.env` المحلي، اضبط:
  ```bash
  ADMIN_PASSWORD="dev123"  # أو اتركه فارغاً للاستخدام مع "dev" في التطوير
  ```

### 2️⃣ Middleware لحماية `/admin/*`
- [ ] أنشئ `src/middleware.admin.ts` (أو حدّث `src/middleware.ts` الحالي):

> ⚠️ **مهم:** الـ middleware الحالي يخص `next-intl`. يجب دمج حماية `/admin` بشكل ذكي. الحل: استخدم Server Component checks بدلاً من middleware (لأن middleware لا يصل لـ cookies بشكل async في Next.js 16 بسهولة).

- [ ] **البديل الأفضل:** استخدم `requireAuth()` في كل صفحة/Server Action في `/admin`:

```tsx
// في كل صفحة admin:
import { requireAuth } from '@/lib/auth';
import { redirect } from 'next/navigation';

export default async function AdminPage() {
  try {
    await requireAuth();
  } catch {
    redirect('/admin/login');
  }
  // ... rest of page
}
```

### 3️⃣ صفحة تسجيل الدخول
- [ ] أنشئ `src/app/[locale]/admin/login/page.tsx` (Server Component wrapper):

```tsx
import { LoginPageView } from '@/components/admin/login-page-view';
import { isAuthenticated } from '@/lib/auth';
import { redirect } from 'next/navigation';

export default async function LoginAdminPage() {
  // If already authenticated, redirect to dashboard
  const authed = await isAuthenticated();
  if (authed) redirect('/admin');
  
  return <LoginPageView />;
}
```

- [ ] أنشئ `src/components/admin/login-page-view.tsx` (Client Component):

**المحتوى:**
- خلفية `--c-bg-dark` (أسود فاخر)
- بطاقة تسجيل دخول في الوسط
- شعار "Last Unique Touch" + "لوحة التحكم"
- نموذج:
  - حقل كلمة المرور (password)
  - زر "تسجيل الدخول"
- استدعِ Server Action `loginAction(password)` عند الإرسال
- عند النجاح: `router.push('/admin')`
- عند الفشل: اعرض رسالة "كلمة المرور غير صحيحة"

### 4️⃣ Server Actions للـ Auth
- [ ] أنشئ `src/app/[locale]/admin/login/actions.ts`:

```typescript
'use server';

import { login, logout } from '@/lib/auth';
import { revalidatePath } from 'next/cache';

export async function loginAction(formData: FormData): Promise<{ success: boolean; error?: string }> {
  const password = formData.get('password') as string;
  if (!password) return { success: false, error: 'missing_password' };
  
  const success = await login(password);
  if (!success) return { success: false, error: 'invalid_password' };
  
  revalidatePath('/admin');
  return { success: true };
}

export async function logoutAction(): Promise<void> {
  await logout();
  revalidatePath('/admin');
}
```

### 5️⃣ Admin Layout + Sidebar
- [ ] أنشئ `src/app/[locale]/admin/layout.tsx`:

```tsx
import { requireAuth } from '@/lib/auth';
import { redirect } from 'next/navigation';
import { AdminShell } from '@/components/admin/admin-shell';

export default async function AdminLayout({ children }: { children: React.ReactNode }) {
  try {
    await requireAuth();
  } catch {
    redirect('/admin/login');
  }
  
  return <AdminShell>{children}</AdminShell>;
}
```

> ⚠️ **استثناء:** صفحة `/admin/login` يجب ألا تمرّ عبر هذا الـ layout. الحل: ضع صفحة login خارج مجلد `[locale]/admin/`:
> - `src/app/[locale]/admin/login/page.tsx` → انقلها لـ `src/app/admin/login/page.tsx` (بدون locale)
> - أو استخدم **route group**: `src/app/[locale]/admin/(auth)/login/page.tsx` مع layout منفصل

- [ ] أنشئ `src/components/admin/admin-shell.tsx` (Client Component):

**المحتوى:**
- **Sidebar (يمين في RTL = يسار بصرياً):**
  - شعار "Last Unique Touch" + "Admin"
  - روابط التنقّل:
    - 📊 لوحة القيادة → `/admin`
    - 📦 المنتجات → `/admin/products`
    - 🗂️ الفئات → `/admin/categories`
    - 📅 الحجوزات → `/admin/bookings`
    - 🚪 تسجيل الخروج → Server Action `logoutAction()`
- **Topbar:**
  - زر "عرض الموقع" → `/` (يفتح في تبويب جديد)
  - ملف المستخدم (mock — "Admin")
- **Main content area:** `{children}`

**الستايل:**
- Sidebar: خلفية `--c-bg-dark`، نص أبيض، روابط نشطة بخلفية `--c-accent-lut` soft
- Topbar: خلفية بيضاء بحدود `--c-border`
- Responsive: sidebar يتحول لـ drawer على الموبايل (زر hamburger)

### 6️⃣ صفحة Dashboard (`/admin`)
- [ ] أنشئ `src/app/[locale]/admin/page.tsx` (Server Component):

**المحتوى:**

**العنوان:** "لوحة القيادة" / "Dashboard"

**4 بطاقات إحصائية (grid-cols-4):**

| البطاقة | القيمة | الأيقونة | اللون |
|---------|--------|---------|------|
| إجمالي المنتجات | `count` | Package | ذهبي soft |
| الحجوزات المعلّقة | `count` (PENDING) | Clock | أصفر soft |
| الحجوزات المؤكدة | `count` (CONFIRMED) | CheckCircle | أخضر soft |
| الإيرادات (هذا الشهر) | `sum` (CONFIRMED هذا الشهر) | DollarSign | أحمر soft |

**قسم "أحدث الحجوزات":**
- جدول بـ 5 أحدث حجوزات
- أعمدة: اسم الزبون، المنتج، التاريخ، الحالة، الإجمالي
- زر "عرض الكل" → `/admin/bookings`

**قسم "المنتجات منخفضة المخزون":**
- قائمة بالمنتجات ذات `stock <= 2`
- تنبيه بصري (لون أصفر/أحمر)

- [ ] أنشئ `src/lib/admin-stats.ts` للدوال الإحصائية:

```typescript
import { db } from './db';

export async function getAdminStats() {
  const [
    totalProducts,
    pendingBookings,
    confirmedBookings,
    recentBookings,
    lowStockProducts,
    monthlyRevenue,
  ] = await Promise.all([
    db.product.count({ where: { isActive: true } }),
    db.booking.count({ where: { status: 'PENDING' } }),
    db.booking.count({ where: { status: 'CONFIRMED' } }),
    db.booking.findMany({
      take: 5,
      orderBy: { createdAt: 'desc' },
      include: { product: true },
    }),
    db.product.findMany({
      where: { stock: { lte: 2 }, isActive: true },
      take: 5,
    }),
    db.booking.aggregate({
      where: {
        status: 'CONFIRMED',
        createdAt: {
          gte: new Date(new Date().getFullYear(), new Date().getMonth(), 1),
        },
      },
      _sum: { totalAmount: true },
    }),
  ]);

  return {
    totalProducts,
    pendingBookings,
    confirmedBookings,
    recentBookings,
    lowStockProducts,
    monthlyRevenue: monthlyRevenue._sum.totalAmount || 0,
  };
}
```

### 7️⃣ إدارة المنتجات (CRUD)

- [ ] أنشئ `src/app/[locale]/admin/products/page.tsx` (Server Component):

**المحتوى:**
- عنوان: "المنتجات" + زر "إضافة منتج" → `/admin/products/new`
- بحث نصي + فلترة بالفئة + فلترة بـ active/inactive
- جدول المنتجات:
  - الصورة (thumbnail)
  - الاسم (nameAr/nameEn حسب الـ locale)
  - الفئة
  - سعر الإيجار/اليوم
  - المخزون
  - الحالة (نشط/غير نشط)
  - إجراءات: تعديل، حذف

- [ ] أنشئ `src/app/[locale]/admin/products/new/page.tsx`:

```tsx
import { requireAuth } from '@/lib/auth';
import { ProductForm } from '@/components/admin/product-form';
import { getCategoriesByBrand } from '@/lib/products';

export default async function NewProductPage() {
  await requireAuth();
  const categories = await getCategoriesByBrand();
  return <ProductForm categories={categories} mode="create" />;
}
```

- [ ] أنشئ `src/app/[locale]/admin/products/[id]/edit/page.tsx`:

```tsx
import { requireAuth } from '@/lib/auth';
import { ProductForm } from '@/components/admin/product-form';
import { getProductById, getCategoriesByBrand } from '@/lib/products';
import { notFound } from 'next/navigation';

interface PageProps {
  params: Promise<{ id: string }>;
}

export default async function EditProductPage({ params }: PageProps) {
  await requireAuth();
  const { id } = await params;
  const [product, categories] = await Promise.all([
    getProductById(id),
    getCategoriesByBrand(),
  ]);
  
  if (!product) notFound();
  
  return <ProductForm categories={categories} product={product} mode="edit" />;
}
```

- [ ] أنشئ `src/components/admin/product-form.tsx` (Client Component):

**الحقول:**

| الحقل | النوع | ملاحظات |
|-------|------|--------|
| الاسم (عربي) | text | مطلوب |
| الاسم (إنجليزي) | text | مطلوب |
| الوصف (عربي) | textarea | مطلوب |
| الوصف (إنجليزي) | textarea | مطلوب |
| الفئة | select | من القائمة |
| سعر الإيجار/اليوم | number | مطلوب، > 0 |
| التأمين | number | مطلوب، >= 0 |
| المخزون | number | مطلوب، >= 0 |
| صور (URLs) | dynamic list | يمكن إضافة/حذف URLs |
| URL نموذج 3D | text | اختياري |
| نشط | checkbox | default: true |
| Slug | text | يُولّد تلقائياً من nameEn، يمكن تعديله |

**السلوك:**
- استخدم `react-hook-form` + `zod`
- في وضع "create": استدعِ Server Action `createProductAction`
- في وضع "edit": استدعِ Server Action `updateProductAction`
- عند النجاح: `router.push('/admin/products')` + toast
- عند الفشل: اعرض الأخطاء

- [ ] أنشئ `src/app/[locale]/admin/products/actions.ts` (Server Actions):

```typescript
'use server';

import { z } from 'zod';
import { db } from '@/lib/db';
import { requireAuth } from '@/lib/auth';
import { stringifyImages } from '@/lib/cart';
import { revalidatePath } from 'next/cache';
import { redirect } from 'next/navigation';

const productSchema = z.object({
  nameAr: z.string().min(1).max(200),
  nameEn: z.string().min(1).max(200),
  descriptionAr: z.string().min(1),
  descriptionEn: z.string().min(1),
  categoryId: z.string().min(1),
  rentalPricePerDay: z.number().positive(),
  securityDeposit: z.number().nonnegative(),
  stock: z.number().int().nonnegative(),
  images: z.array(z.string().url()).min(1, 'at_least_one_image'),
  model3dUrl: z.string().url().optional().or(z.literal('')),
  isActive: z.boolean(),
  slug: z.string().min(1).max(200).regex(/^[a-z0-9-]+$/, 'invalid_slug'),
});

export async function createProductAction(formData: FormData): Promise<{ success: boolean; error?: string }> {
  await requireAuth();
  
  const parsed = productSchema.safeParse({
    nameAr: formData.get('nameAr'),
    nameEn: formData.get('nameEn'),
    descriptionAr: formData.get('descriptionAr'),
    descriptionEn: formData.get('descriptionEn'),
    categoryId: formData.get('categoryId'),
    rentalPricePerDay: parseFloat(formData.get('rentalPricePerDay') as string),
    securityDeposit: parseFloat(formData.get('securityDeposit') as string),
    stock: parseInt(formData.get('stock') as string, 10),
    images: JSON.parse(formData.get('images') as string),
    model3dUrl: formData.get('model3dUrl') || '',
    isActive: formData.get('isActive') === 'true',
    slug: formData.get('slug'),
  });
  
  if (!parsed.success) {
    return { success: false, error: 'invalid_input' };
  }
  
  try {
    // Check slug uniqueness
    const existing = await db.product.findUnique({ where: { slug: parsed.data.slug } });
    if (existing) {
      return { success: false, error: 'slug_exists' };
    }
    
    await db.product.create({
      data: {
        ...parsed.data,
        brand: 'LUT',
        model3dUrl: parsed.data.model3dUrl || null,
        images: stringifyImages(parsed.data.images),
      },
    });
    
    revalidatePath('/admin/products');
    redirect('/admin/products');
  } catch (error) {
    console.error('Create product error:', error);
    return { success: false, error: 'internal_error' };
  }
}

export async function updateProductAction(id: string, formData: FormData): Promise<{ success: boolean; error?: string }> {
  await requireAuth();
  // ... similar logic, but update instead of create
}

export async function deleteProductAction(id: string): Promise<{ success: boolean; error?: string }> {
  await requireAuth();
  
  try {
    // Soft delete — set isActive to false (don't actually delete to preserve booking history)
    await db.product.update({
      where: { id },
      data: { isActive: false },
    });
    
    revalidatePath('/admin/products');
    return { success: true };
  } catch (error) {
    console.error('Delete product error:', error);
    return { success: false, error: 'internal_error' };
  }
}
```

> ⚠️ **قواعد أمنية:**
> - **#7**: استدعِ `requireAuth()` في كل Server Action
> - **#10**: تحقق من ملكية المورد (هنا نتحقق أن المنتج من brand=LUT)
> - **#14**: try/catch شامل
> - **#15**: لا تُسرّب تفاصيل الخطأ
> - **#16**: Zod schema

### 8️⃣ إدارة الفئات (CRUD بسيط)
- [ ] أنشئ `src/app/[locale]/admin/categories/page.tsx`:

**المحتوى:**
- قائمة الفئات في جدول بسيط
- زر "إضافة فئة" → modal (ليس صفحة منفصلة)
- لكل فئة: الاسم (عربي/إنجليزي)، Slug، عدد المنتجات، إجراءات (تعديل، حذف)
- الحذف: تحقق أولاً أنه لا توجد منتجات في الفئة

- [ ] أنشئ Server Actions في `src/app/[locale]/admin/categories/actions.ts`:
  - `createCategoryAction`
  - `updateCategoryAction`
  - `deleteCategoryAction` (مع فحص المنتجات المرتبطة)

### 9️⃣ إدارة الحجوزات
- [ ] أنشئ `src/app/[locale]/admin/bookings/page.tsx` (Server Component):

**المحتوى:**
- عنوان: "الحجوزات"
- فلترة بالحالة (chips): الكل، PENDING، CONFIRMED، CANCELLED، COMPLETED
- فلترة بالتاريخ (from/to)
- بحث برقم الهاتف أو البريد
- جدول الحجوزات:
  - رقم الحجز (أول 8 أحرف من ID)
  - اسم الزبون
  - المنتج
  - فترة الإيجار
  - الإجمالي
  - الحالة (badge ملوّن)
  - إجراء: "عرض" → `/admin/bookings/[id]`

- [ ] أنشئ `src/app/[locale]/admin/bookings/[id]/page.tsx`:

**المحتوى:**
- تفاصيل الحجز الكاملة
- بيانات الزبون (الاسم، الهاتف، البريد، العنوان، المدينة، ملاحظات)
- تفاصيل المنتج
- فترة الإيجار + عدد الأيام
- الملخص المالي (إيجار + تأمين + إجمالي)
- **أزرار تغيير الحالة:**
  - PENDING → CONFIRMED (زر أخضر)
  - PENDING/CANCELLED → CANCELLED (زر أحمر)
  - CONFIRMED → COMPLETED (زر أزرق)
- سجل التغييرات (إن أمكن — أو سجّل في SecurityLog)

- [ ] أنشئ Server Actions في `src/app/[locale]/admin/bookings/actions.ts`:

```typescript
'use server';

import { z } from 'zod';
import { db } from '@/lib/db';
import { requireAuth } from '@/lib/auth';
import { revalidatePath } from 'next/cache';

const statusSchema = z.object({
  bookingId: z.string().min(1),
  newStatus: z.enum(['PENDING', 'CONFIRMED', 'CANCELLED', 'COMPLETED']),
});

export async function updateBookingStatusAction(
  bookingId: string,
  newStatus: 'PENDING' | 'CONFIRMED' | 'CANCELLED' | 'COMPLETED'
): Promise<{ success: boolean; error?: string }> {
  await requireAuth();
  
  try {
    const booking = await db.booking.findUnique({ where: { id: bookingId } });
    if (!booking) return { success: false, error: 'not_found' };
    
    // Validate status transition
    const validTransitions: Record<string, string[]> = {
      PENDING: ['CONFIRMED', 'CANCELLED'],
      CONFIRMED: ['COMPLETED', 'CANCELLED'],
      CANCELLED: [],
      COMPLETED: [],
    };
    
    if (!validTransitions[booking.status]?.includes(newStatus)) {
      return { success: false, error: 'invalid_transition' };
    }
    
    await db.booking.update({
      where: { id: bookingId },
      data: { status: newStatus },
    });
    
    // Log the change
    await db.securityLog.create({
      data: {
        event: 'booking_status_changed',
        details: JSON.stringify({
          bookingId,
          from: booking.status,
          to: newStatus,
        }),
      },
    });
    
    revalidatePath('/admin/bookings');
    revalidatePath(`/admin/bookings/${bookingId}`);
    return { success: true };
  } catch (error) {
    console.error('Update booking status error:', error);
    return { success: false, error: 'internal_error' };
  }
}
```

### 🔟 Toast Notifications
- [ ] أنشئ `src/components/providers/toast-provider.tsx` (Client Component):

```tsx
'use client';

import { createContext, useContext, useState, useCallback } from 'react';
import { CheckCircle2, XCircle, AlertCircle } from 'lucide-react';

type ToastType = 'success' | 'error' | 'warning';
interface Toast {
  id: string;
  type: ToastType;
  message: string;
}

const ToastContext = createContext<{
  showToast: (type: ToastType, message: string) => void;
} | null>(null);

export function ToastProvider({ children }: { children: React.ReactNode }) {
  const [toasts, setToasts] = useState<Toast[]>([]);

  const showToast = useCallback((type: ToastType, message: string) => {
    const id = Math.random().toString(36).substr(2, 9);
    setToasts((prev) => [...prev, { id, type, message }]);
    setTimeout(() => {
      setToasts((prev) => prev.filter((t) => t.id !== id));
    }, 4000);
  }, []);

  return (
    <ToastContext.Provider value={{ showToast }}>
      {children}
      <div className="fixed bottom-4 end-4 z-50 space-y-2">
        {toasts.map((toast) => (
          <div
            key={toast.id}
            className={`flex items-center gap-3 px-4 py-3 rounded-lg shadow-lg text-white ${
              toast.type === 'success' ? 'bg-green-600' :
              toast.type === 'error' ? 'bg-red-600' :
              'bg-yellow-600'
            }`}
          >
            {toast.type === 'success' && <CheckCircle2 className="w-5 h-5" />}
            {toast.type === 'error' && <XCircle className="w-5 h-5" />}
            {toast.type === 'warning' && <AlertCircle className="w-5 h-5" />}
            <span className="text-sm">{toast.message}</span>
          </div>
        ))}
      </div>
    </ToastContext.Provider>
  );
}

export function useToast() {
  const ctx = useContext(ToastContext);
  if (!ctx) throw new Error('useToast must be used within ToastProvider');
  return ctx;
}
```

- [ ] لفّ الـ `[locale]/layout.tsx` بـ `<ToastProvider>`:

```tsx
<CartProvider>
  <ToastProvider>
    {children}
  </ToastProvider>
</CartProvider>
```

### 1️⃣1️⃣ تأكيد الحذف (Modal)
- [ ] أنشئ `src/components/admin/confirm-delete.tsx` (Client Component):

**المحتوى:**
- Modal بسيط مع:
  - عنوان: "تأكيد الحذف"
  - نص: "هل أنت متأكد من حذف [اسم المنتج]؟"
  - زر "إلغاء" (ثانوي)
  - زر "حذف" (أحمر)
- يستقبل `onConfirm` callback

### 1️⃣2️⃣ تحديث ملفات الترجمة
- [ ] أضف كل المفاتيح اللازمة لـ `messages/ar.json` و `messages/en.json`:

```json
{
  "admin": {
    "title": "لوحة التحكم",
    "login": {
      "title": "تسجيل الدخول",
      "password": "كلمة المرور",
      "submit": "دخول",
      "invalid": "كلمة المرور غير صحيحة",
      "devHint": "في التطوير، استخدم كلمة المرور \"dev\""
    },
    "nav": {
      "dashboard": "لوحة القيادة",
      "products": "المنتجات",
      "categories": "الفئات",
      "bookings": "الحجوزات",
      "logout": "تسجيل الخروج",
      "viewSite": "عرض الموقع"
    },
    "dashboard": {
      "title": "لوحة القيادة",
      "stats": {
        "totalProducts": "إجمالي المنتجات",
        "pendingBookings": "حجوزات معلّقة",
        "confirmedBookings": "حجوزات مؤكدة",
        "monthlyRevenue": "الإيرادات (هذا الشهر)"
      },
      "recentBookings": "أحدث الحجوزات",
      "lowStock": "منتجات منخفضة المخزون",
      "viewAll": "عرض الكل"
    },
    "products": {
      "title": "المنتجات",
      "add": "إضافة منتج",
      "edit": "تعديل",
      "delete": "حذف",
      "search": "بحث...",
      "filterCategory": "كل الفئات",
      "filterStatus": "كل الحالات",
      "active": "نشط",
      "inactive": "غير نشط",
      "stock": "المخزون",
      "price": "السعر/يوم",
      "actions": "إجراءات",
      "form": {
        "titleNew": "إضافة منتج جديد",
        "titleEdit": "تعديل المنتج",
        "nameAr": "الاسم (عربي)",
        "nameEn": "الاسم (إنجليزي)",
        "descriptionAr": "الوصف (عربي)",
        "descriptionEn": "الوصف (إنجليزي)",
        "category": "الفئة",
        "rentalPrice": "سعر الإيجار/اليوم",
        "securityDeposit": "التأمين",
        "stock": "المخزون",
        "images": "صور (URLs)",
        "addImage": "إضافة صورة",
        "model3dUrl": "URL نموذج 3D (اختياري)",
        "isActive": "نشط",
        "slug": "Slug",
        "submit": "حفظ",
        "cancel": "إلغاء"
      },
      "deleted": "تم حذف المنتج",
      "saved": "تم حفظ المنتج",
      "confirmDelete": "هل أنت متأكد من حذف \"{name}\"؟"
    },
    "categories": {
      "title": "الفئات",
      "add": "إضافة فئة",
      "nameAr": "الاسم (عربي)",
      "nameEn": "الاسم (إنجليزي)",
      "slug": "Slug",
      "productCount": "عدد المنتجات",
      "actions": "إجراءات",
      "cannotDelete": "لا يمكن حذف فئة تحتوي على منتجات",
      "saved": "تم حفظ الفئة",
      "deleted": "تم حذف الفئة"
    },
    "bookings": {
      "title": "الحجوزات",
      "filterStatus": {
        "all": "الكل",
        "PENDING": "معلّق",
        "CONFIRMED": "مؤكد",
        "CANCELLED": "ملغي",
        "COMPLETED": "مكتمل"
      },
      "table": {
        "id": "رقم الحجز",
        "customer": "الزبون",
        "product": "المنتج",
        "period": "فترة الإيجار",
        "total": "الإجمالي",
        "status": "الحالة",
        "actions": "إجراءات"
      },
      "detail": {
        "title": "تفاصيل الحجز",
        "customerInfo": "بيانات الزبون",
        "name": "الاسم",
        "phone": "الهاتف",
        "email": "البريد",
        "address": "العنوان",
        "city": "المدينة",
        "notes": "ملاحظات",
        "productInfo": "بيانات المنتج",
        "rentalInfo": "بيانات الإيجار",
        "startDate": "تاريخ البداية",
        "endDate": "تاريخ النهاية",
        "days": "عدد الأيام",
        "financialSummary": "الملخص المالي",
        "rental": "الإيجار",
        "deposit": "التأمين",
        "total": "الإجمالي",
        "changeStatus": "تغيير الحالة",
        "confirm": "تأكيد",
        "cancel": "إلغاء",
        "complete": "إكمال"
      },
      "statusChanged": "تم تغيير حالة الحجز",
      "invalidTransition": "لا يمكن تغيير الحالة بهذا الشكل"
    },
    "common": {
      "save": "حفظ",
      "cancel": "إلغاء",
      "delete": "حذف",
      "edit": "تعديل",
      "view": "عرض",
      "confirm": "تأكيد",
      "back": "رجوع",
      "loading": "جارٍ التحميل...",
      "noData": "لا توجد بيانات",
      "error": "حدث خطأ"
    },
    "errors": {
      "invalid_input": "بيانات غير صالحة",
      "slug_exists": "الـ Slug مستخدم مسبقاً",
      "not_found": "غير موجود",
      "internal_error": "حدث خطأ — يرجى المحاولة لاحقاً",
      "unauthorized": "غير مصرّح"
    }
  }
}
```

---

## ✅ قائمة التحقق النهائية

### الوظائف
- [ ] `/admin/login` تعرض صفحة تسجيل الدخول
- [ ] تسجيل الدخول بـ "dev" (في التطوير) يعمل
- [ ] `/admin` محمية — تُعيد توجيه لـ login إن لم يكن مسجّلاً
- [ ] `/admin` تعرض Dashboard بالإحصائيات
- [ ] `/admin/products` تعرض قائمة المنتجات
- [ ] `/admin/products/new` تعرض نموذج إضافة منتج
- [ ] إضافة منتج يعمل (Server Action)
- [ ] `/admin/products/[id]/edit` تعرض نموذج التعديل
- [ ] تعديل منتج يعمل
- [ ] حذف منتج (soft delete) يعمل
- [ ] `/admin/categories` تعرض الفئات
- [ ] إضافة/تعديل/حذف فئة يعمل
- [ ] `/admin/bookings` تعرض قائمة الحجوزات
- [ ] فلترة الحجوزات بالحالة تعمل
- [ ] `/admin/bookings/[id]` تعرض تفاصيل الحجز
- [ ] تغيير حالة الحجز يعمل (مع valid transitions)
- [ ] تسجيل الخروج يعمل
- [ ] Toast notifications تظهر عند النجاح/الفشل
- [ ] تأكيد الحذف modal يعمل

### الأمان
- [ ] `requireAuth()` في كل صفحة و Server Action
- [ ] Cookie httpOnly + secure + sameSite
- [ ] HMAC signature على session token
- [ ] Zod schema على كل input
- [ ] try/catch شامل
- [ ] لا تُسرّب تفاصيل الخطأ
- [ ] Soft delete للمنتجات (يحافظ على سجل الحجوزات)
- [ ] فحص المنتجات المرتبطة قبل حذف فئة
- [ ] Valid status transitions للحجوزات

### UX
- [ ] Sidebar navigation احترافي
- [ ] Responsive على الموبايل (drawer للـ sidebar)
- [ ] Toast notifications أنيقة
- [ ] تأكيدات الحذف
- [ ] Loading states
- [ ] Empty states (لا توجد منتجات/حجوزات)

### i18n
- [ ] كل النصوص في `messages/*.json`
- [ ] لا hardcoded strings
- [ ] RTL/LTR ديناميكي

### التشغيل
- [ ] `bun dev --webpack -p 3000` يعمل بدون أخطاء
- [ ] `bun run lint` — لا أخطاء
- [ ] `bun run typecheck` — لا أخطاء
- [ ] `bun run build` — ينجح بدون MISSING_MESSAGE

---

## 📝 التقرير المطلوب

### 1. ملخص التنفيذ
### 2. المكوّنات والصفحات المُنشأة
```
src/lib/
├── auth.ts                       ← Mock auth (cookie + HMAC)
└── admin-stats.ts                ← Dashboard statistics

src/components/admin/
├── admin-shell.tsx               ← Layout with sidebar
├── login-page-view.tsx           ← Login form
├── product-form.tsx              ← Create/Edit product form
├── confirm-delete.tsx            ← Delete confirmation modal
└── (bookings-table, categories-table, etc.)

src/components/providers/
└── toast-provider.tsx            ← Toast notifications

src/app/[locale]/admin/
├── layout.tsx                    ← Protected layout
├── page.tsx                      ← Dashboard
├── login/page.tsx                ← Login page (excluded from layout)
├── products/
│   ├── page.tsx                  ← List
│   ├── new/page.tsx              ← Create
│   ├── [id]/edit/page.tsx        ← Edit
│   └── actions.ts                ← Server Actions
├── categories/
│   ├── page.tsx                  ← List + modal
│   └── actions.ts                ← Server Actions
└── bookings/
    ├── page.tsx                  ← List
    ├── [id]/page.tsx             ← Detail
    └── actions.ts                ← Server Actions
```

### 3. لقطات شاشة (إلزامية)
- لقطة لـ `/admin/login`
- لقطة لـ `/admin` (Dashboard)
- لقطة لـ `/admin/products` (قائمة)
- لقطة لـ `/admin/products/new` (نموذج إضافة)
- لقطة لـ `/admin/products/[id]/edit` (نموذج تعديل)
- لقطة لـ `/admin/categories`
- لقطة لـ `/admin/bookings` (قائمة مع فلترة)
- لقطة لـ `/admin/bookings/[id]` (تفاصيل + أزرار تغيير الحالة)
- لقطة لـ Toast notification (نجاح)
- لقطة لـ Modal تأكيد الحذف
- لقطة للموبايل (375px)

### 4. نتائج الفحوصات
```
bun run lint:        ✅ / ❌
bun run typecheck:   ✅ / ❌
bun run build:       ✅ / ❌
bun dev --webpack:   ✅ / ❌
```

### 5. اختبارات وظيفية
| الاختبار | النتيجة |
|----------|---------|
| تسجيل الدخول بـ "dev" | ✅ / ❌ |
| حماية `/admin` (redirect لـ login) | ✅ / ❌ |
| إضافة منتج | ✅ / ❌ |
| تعديل منتج | ✅ / ❌ |
| حذف منتج (soft delete) | ✅ / ❌ |
| إضافة فئة | ✅ / ❌ |
| حذف فئة (مع منتجات) | ✅ (ممنوع) / ❌ |
| فلترة الحجوزات | ✅ / ❌ |
| تغيير حالة حجز | ✅ / ❌ |
| Invalid transition | ✅ (مرفوض) / ❌ |
| تسجيل الخروج | ✅ / ❌ |
| Toast notifications | ✅ / ❌ |

### 6. ملاحظات للمستخدم
- تأكيد أن mock auth سيعمل في التطوير بكلمة مرور "dev"
- تأكيد أن Clerk سيُضاف في المرحلة 10 (مع الترحيل لـ Supabase)
- ملاحظة أن رفع الصور سيُضاف في المرحلة 10 (الآن URL input فقط)

---

## ⚠️ قواعد صارمة

1. **🚨 لا تستخدم Clerk** — mock auth فقط (cookie + HMAC).
2. **لا تتجاوز نطاق هذه المرحلة** — لا تبنِ اختبارات (المرحلة 9)، لا ترحيل لـ Supabase (المرحلة 10).
3. **استخدم `bun dev --webpack -p 3000`** لتشغيل المشروع.
4. **`requireAuth()` في كل صفحة و Server Action** (يمنع خطأ أمني #7).
5. **Cookie: httpOnly + secure + sameSite** (يمنع XSS).
6. **HMAC signature على session token** (يمنع التزوير).
7. **Zod schema على كل input** (يمنع خطأ أمني #16).
8. **try/catch شامل** (يمنع خطأ أمني #14).
9. **لا تُسرّب تفاصيل الخطأ** (يمنع خطأ أمني #15).
10. **Soft delete للمنتجات** — لا تحذف فعلياً (يحافظ على سجل الحجوزات).
11. **فحص المنتجات قبل حذف فئة** — امنع الحذف إن وُجدت منتجات.
12. **Valid status transitions** — لا تسمح بانتقالات غير منطقية.
13. **استخدم Server Actions** للتغييرات في DB (أنظف من API routes للوحة التحكم).
14. **`next/image` لا يقبل `undefined`** — conditional rendering.
15. **لا تستخدم `next/link` مباشرة** — استخدم `Link` من `@/i18n/routing`.
16. **استخدم logical properties** للـ RTL/LTR.
17. **لا إيموجي Unicode** — استخدم أيقونات lucide-react.
18. **TypeScript strict** — لا `any`.
19. **اقرأ الكود الحالي قبل البدء** — خصوصاً `src/lib/products.ts` و `src/lib/cart.ts`.
20. **اقرأ `dev.log` بعد كل تغيير كبير**.
21. **ابدأ Agent Browser قبل الإبلاغ بالانتهاء** — اختبر الرحلة الكاملة بصرياً.

---

## 🎯 معيار النجاح

المهمة تُعتبر مكتملة إذا وفقط إذا:
1. ✅ `/admin/login` تعمل (تسجيل دخول بـ "dev")
2. ✅ `/admin` محمية وتعرض Dashboard
3. ✅ CRUD المنتجات يعمل كاملاً
4. ✅ CRUD الفئات يعمل كاملاً
5. ✅ `/admin/bookings` تعرض القائمة مع فلترة
6. ✅ `/admin/bookings/[id]` تعرض التفاصيل + تغيير الحالة
7. ✅ Valid status transitions مُطبّقة
8. ✅ Toast notifications تظهر
9. ✅ تأكيدات الحذف تعمل
10. ✅ تسجيل الخروج يعمل
11. ✅ `bun run build` ينجح بدون MISSING_MESSAGE
12. ✅ لا أخطاء TypeScript أو ESLint

ابدأ الآن. أبلغني فور الانتهاء بالتقرير الكامل مع لقطات الشاشة.
````

---

## 📌 كيف نتابع؟

1. **انسخ البرومبت أعلاه** وأرسله للـ Full Stack Agent.
2. **انتظر التقرير الكامل** مع لقطات الشاشة للوحة التحكم.
3. **أرسل لي التقرير** — سأراجعه، وإن كان كل شيء سليماً سأعطيك **البرومبت التاسع** (SEO + اختبارات + a11y + تحسين الأداء).

أنا في الانتظار 🚀